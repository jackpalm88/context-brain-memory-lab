DATABASE_URL=postgresql://postgres:postgres@localhost:5432/memory_lab
LLM_PROVIDER=none
EMBEDDING_PROVIDER=none
# ANTHROPIC_API_KEY=sk-ant-...   # optional — provider key is not required; no-key fallback scoring works without it
